export interface NavItem {
  label: string;
  href: string;
}

export interface Service {
  title: string;
  description: string;
  outcomes: string[];
}

export interface Project {
  name: string;
  summary: string;
  tags: string[];
}
