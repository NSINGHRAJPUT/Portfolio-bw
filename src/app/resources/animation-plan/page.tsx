import { AnimationPlan } from "@/components/shared/animation-plan";

export const metadata = {
  title: "Animation Plan | NSRGFX",
  description: "Framer-first animation architecture and cinematic motion plan for NSRGFX.",
};

export default function AnimationPlanPage() {
  return <AnimationPlan />;
}
